"""Built-in search backends for oks-connector.

Third-party backends (e.g. codegraph) can register independently via
entry_points(group="oks_search_backend"). This package ships `code` as a
built-in example + practical backend.
"""
